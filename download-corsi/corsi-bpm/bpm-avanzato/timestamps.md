# BPM Avanzato — Timestamp Index

Course: "BPM Avanzato" (Digit Company BPM Designer, advanced/follow-up course). Full recording ≈ 5h17m. Timestamps reference `[HH:MM:SS]` marks in the Whisper transcript. Descriptions are paraphrased/translated from the Italian audio.

| Timestamp | Category | Title | Description |
|---|---|---|---|
| [00:15:53] | concept-intro | Data grid variable type introduced | A "griglia dati" (data grid) is introduced as a special process variable: not a normal field or a group, but a read-only grid bound to a table. |
| [00:18:07] | demo-start | Linking a data grid to a table | Demo of binding a data grid to the "fornitori" (suppliers) table and choosing which columns (codice, descrizione) to expose, with custom column captions. |
| [00:27:06] | tutorial-step | Configure a per-task filter on a group variable | In "variabili da richiedere," the group's local settings allow disabling add/delete and applying a filter (e.g. "tipo preventivo = A") so one task only shows a subset of group rows. |
| [00:30:43] | tip | Helper variable workaround for debugging formulas | Since BPM has no debugger, the instructor recommends creating throwaway "presenza tipo A / tipo B" variables in the warehouse just to visualize intermediate formula results while designing logic. |
| [00:34:15] | tutorial-step | VB-script loop over group rows (worked example) | Demo of a Visual Basic formula that declares an Integer, loops through "lista preventivi," and checks `tipo preventivo(i) = "tipo A"` to detect whether at least one row of a given type exists. |
| [00:39:44] | definition | Global validation formula vs field-level validation | Field-level "formula di validazione" checks a single field; a separate global validation formula (evaluated when the activity is marked completed) validates the whole screen at once. |
| [00:43:44] | definition | Legacy sync bar object ("sbarra di sincronizzazione") | The classic gateway object that behaves like a gate: it only opens once all incoming parallel branches have arrived, used to join two parallel activities before continuing. |
| [00:45:34] | tip | ALL vs ALL PLANNED gotcha on sync bars | If a sync bar is configured with "ALL" it waits for every configured incoming branch regardless of whether it actually started; "ALL PLANNED" (all launched) only waits for branches that were actually triggered — important when parallel paths are conditional. |
| [00:47:00] | definition | Three modern gateway types: exclusive, parallel, inclusive | BPMN-style gateway objects: exclusive (percorsi alternativi, one path out, mutually exclusive), parallel (percorsi paralleli, always all paths), inclusive (percorsi liberi, one, several, or all paths based on conditions). |
| [00:52:39] | concept-intro | Purely graphical objects: text box, image, page setup | Text boxes (with color/font/border styling), logo images, and page/margins settings let you make a process diagram presentable and printable (e.g. to PDF), purely cosmetic and non-functional. |
| [00:58:20] | definition | Marker object for multi-page process navigation | The "marker" acts as a teleport/goto between pages when a process diagram is split across multiple pages (useful for very large/long processes kept readable and printable). |
| [01:00:10] | concept-intro | Swim lanes introduced | Swim lanes are adjacent, ordered "tracks" (like a spreadsheet's columns) used to lay out a process by organizational responsibility (department/role), as required by strict BPMN diagrams. |
| [01:06:00] | definition | Swim lane inherits "utenti responsabili" to its tasks | Setting responsible users/groups at the swim-lane level applies to any task placed in that lane that doesn't have its own explicit assignment (assignment at task level overrides the lane). |
| [01:07:28] | example | Real project: zig-zag swim-lane process | Instructor shows a real multi-department process (commerciale/tecnico/acquisti/qualità/produzione) laid out with swim lanes, noting it's visually clear about "who does what" but can get visually tangled ("ingarbugliato") to draw. |
| [01:15:37] | concept-intro | Multiple start events in one process | A process can have two or more distinct Start objects (e.g. normal request vs. security request), each with its own set of requested variables/validation rules; at launch the system asks which start point to use. |
| [01:16:00] | demo-start | Building two start events: normal vs. security | Demo of adding a second Start ("inserimento richiesta sicurezza") with its own default values and required fields, separate from the normal request start. |
| [01:22:05] | definition | "Start a tempo" — scheduled/timed start event | A start event that fires the process automatically on a schedule (e.g. first Monday of the month at 8:00) instead of waiting for a user action — useful for periodic/administrative processes; the first real activity in the process still needs to gather the actual data since the timed start carries no variables. |
| [01:23:44] | definition | "Attesa" (wait) event | An event object that suspends a process for a configured duration (e.g. "wait 5 days") and then automatically proceeds to the next step, without producing a to-do-list entry during the wait. |
| [01:26:00] | definition | "Fine" vs "Termina Processo" distinction | "Fine" is a purely graphical end-of-branch marker; "Termina Processo" actually kills the whole process instance, closing any still-open parallel activities — used for exception/kill branches. |
| [01:28:10] | definition | Timeout/escalation boundary event | An event attached to the border of a task (not the main flow) that fires an alternate path (e.g. reminder or escalation) after N days from the task's activation/due date; functionally identical whether labeled "timeout" or "escalation" — the naming is just semantic. Matches BPMN's "boundary event" concept. |
| [01:30:34] | concept-intro | "Operations" — automatic system activities | Distinct from user tasks (rectangles) and events (small circles), "operations" are automated actions the BPM engine performs itself (send mail, create linked process, run SQL, etc.), draggable into the flow. |
| [01:34:06] | tutorial-step | Building an email notification template | Demo of creating a new mail template (subject, body with process variables via right-click, recipients as fixed user/group or via a variable, attachments/reports) that an "invio mail" operation then references. |
| [01:41:00] | definition | "Attivazione" vs "inizio" (activation vs start) | Activation = the moment the task becomes available/visible in someone's to-do list; "inizio" = the separate, explicit moment a user declares they've begun working on it (tracked only if "rileva inizio" is enabled). |
| [01:46:14] | tip | "Link web al task" generic field | Inside a mail template body you can insert a special placeholder that becomes a direct deep link to the web BPM task — clicking it opens the activity directly, bypassing the to-do list. |
| [01:50:03] | concept-intro | "Nuovo processo collegato" (linked/child process) operation | An operation that launches a separate, independently-identified BPM process instance from within the current one, linked for navigation but with its own separate variable set. |
| [01:52:41] | demo-start | Building a linked process: "gestione collaudo" | Live demo creating a small "test/collaudo" process and configuring the linked-process operation to launch it, passing variables. |
| [01:58:47] | tutorial-step | Formula-driven variable mapping on launch | Beyond simple mapping, a formula can set destination variables based on source ones (e.g. if origin "tipo richiesta" = security, write a note into the destination process). |
| [02:00:00] | definition | "Ritorno al processo chiamante" — required companion object | To use "wait for return" on a linked process, the child process must include a matching "return to calling process" object at its end, which both signals completion and can pass variables/attachments back to the parent. |
| [02:10:20] | definition | Set-variable/formula operation ("inserire un pezzo di scritto") | A lightweight operation that runs a short script setting one or more variables directly, distinct from a full validation/enabling formula. |
| [02:11:19] | example | Clearing a stale variable on a rework loop | Worked example: when an activity is sent back for revision, the previous "esito approvazione" (approval outcome) variable retains its old value; a set-variable operation blanks it so the required-field check forces the user to re-decide. |
| [02:15:17] | example | Auto-assign an approver by amount threshold | Worked example using a Set Variable operation with an if/then formula: if `importo richiesta < 1000` assign the "utente approvazione tecnica" variable to Dir Tech, else to Dir Gen — a common pattern (e.g. purchase-request approval). |
| [02:19:05] | concept-intro | Decision table object introduced | A newer, more declarative alternative to writing formula code: define input columns and output columns and a set of rule rows, and BPM generates the underlying VB code automatically. |
| [02:20:39] | tutorial-step | Decision table demo: amount → approver | Walkthrough of building a 1-input/1-output decision table (importo richiesta → utente approvazione) with rule rows for <1000 vs ≥1000. |
| [02:27:47] | tip | Convert between decision table and formula code | A table can be converted to plain formula code if its logic outgrows what the table can express (e.g. counting group rows), and — conversely — a formula field can be converted into a table to get a structured starting point; described as being "like using ChatGPT" as a jump-start, not a final answer. |
| [02:29:08] | concept-intro | SQL operation object introduced | An operation ("SQL/libero") that executes an arbitrary SQL statement against a configured external connection — full read/write access, including calling stored procedures. |
| [02:30:18] | tutorial-step | SQL update/query demo with in/out parameters | Demo of writing `UPDATE ... SET ... WHERE ... = @n` with an input parameter bound to a process variable, and a separate `SELECT` with an output parameter written back into a process variable. |
| [02:34:16] | definition | External connection strings configuration | Connection strings (used by both the SQL operation and table lookups) are managed centrally under Configuration → Connessione Esterna, supporting SQL Server and ODBC-type targets. |
| [02:35:10] | definition | "Aggiorna un altro processo" operation | Lets you update variables on any other BPM process instance at any point mid-flow (not just at launch/return), even if the two processes were never formally linked — you only need a rule to find the target instance (by internal instance ID or by matching a variable). |
| [02:41:19] | concept-intro | "Get Data" operation introduced | A structured alternative to a raw SQL query: pick a table/view, supply the lookup key(s), and map returned columns straight into process variables — mirrors the same UI used for a field's table lookup. |
| [02:46:02] | concept-intro | Custom local tables ("tabelle P_") introduced | Beyond built-in views, BPM lets you define fully custom lookup tables (prefixed `P_` in SQL) directly inside the designer — useful when a value-list isn't enough (need multiple columns) or the data must be maintained by someone other than the process designer. |
| [02:47:44] | tutorial-step | Demo: creating a custom table "aree geografiche" | Walkthrough of Configuration → Tabelle → Nuova Tabella, defining columns with a variable-like editor, and saving to auto-generate the SQL table `P_area_geografiche`. |
| [02:56:08] | tip | Value list vs. custom table — when to use which | Guidance: a value list belongs inside the process because conditions often depend on its fixed values (changing it can break logic); a custom table is better when there are many options, they need filtering, or they're maintained centrally by someone without process-design permissions. |
| [02:57:37] | concept-intro | Reports & dashboards — two ways to extract data | Introduces the two BPM analytics tools built on process variables: pixel-perfect PDF "reports" and interactive "dashboards" (cruscotti). |
| [02:58:34] | example | Real case study: "scheda commessa" (work-order form) | A detailed real customer example: before BPM, sales reps filled in a blank PDF form by hand and emailed it around; the process now captures ~70-80 fields (many table-driven) directly in BPM instead. |
| [03:02:27] | definition | Report designer — embedded 3rd-party tool | The report designer is a licensed/embedded third-party component (same family as Crystal Reports / Globe's PDF generator / SAM's RGT) that lets you drag process variables onto a PDF-style template to recreate the old paper form. |
| [03:08:03] | concept-intro | SAM connector — integration topic introduced | Introduces the broader topic of connecting BPM to the external ERP "SAM," covering both directions (BPM→SAM and SAM→BPM). |
| [03:10:52] | concept-intro | "Connettore attivo" (active connector) operation | An operation type, tagged per external connector, that writes documents/entities into SAM (e.g. creating a "commessa"/work order) by dragging it into the flow just like a mail operation. |
| [03:13:24] | tutorial-step | XML template ↔ variable mapping demo | Demo of double-clicking the connector, choosing a pre-built XML template (e.g. "xml commessa"), and mapping its exposed input nodes/attributes to process variables (or fixed values). |
| [03:18:49] | definition | Detail/row parameters — mapping a group to XML rows | If the process has a group (e.g. "lista codici"), each row is automatically mapped to a repeated XML detail node (e.g. `csx riga OV`), letting a whole order/BOM line list be pushed to SAM in one call. |
| [03:25:34] | definition | Connector architecture: definition + DLL | A connector has two parts: a metadata "definition" (companies, exposed interfaces/parameters, stored in BPM config) and an external DLL/plugin that must be installed on the environment to actually do the work. |
| [03:34:22] | tip | Active connector requires SAM's Web Import installed | Getting the active connector working requires SAM's Web Import module to be installed and reachable — normally handled as a routine sysadmin ticket, no extra license needed. |
| [03:39:01] | concept-intro | SAM to-do list shows BPM events | A SAM module lets BPM to-do-list items ("eventi BPM") appear inline in SAM's own to-do list for users who work in both systems, useful so they don't need to switch applications. |
| [03:42:38] | example | Bidirectional sync: firma1/firma2 mirrored in both systems | Demo showing that advancing a task in BPM (e.g. from "firma1" to "firma2") is immediately reflected as the same activity in SAM's to-do list — they're literally the same underlying task. |
| [03:44:51] | tutorial-step | User alias mapping between BPM and SAM | Under Users & Groups, each connector can have its own external-user alias mapping (e.g. SAM's `css-admin` ↔ BPM's `admin`), so SAM's to-do list knows which BPM user's activities to show. |
| [03:48:22] | concept-intro | Piloting BPM from SAM — topic introduced | Introduces the reverse integration direction: triggering/driving BPM processes from actions taken inside SAM. |
| [03:50:25] | definition | Standard endpoints: create process, exec task, etc. | Listed endpoints include create-new-process, exec-task (advance an activity in someone's to-do list), upload-attachment, force a process forward, read a process's data, and read a user's to-do list. |
| [03:52:06] | demo-start | Postman demo: create a new process via web service | Live Postman demo calling `api-ext-create-new-process` with an auth token, target model/start point, and JSON variable payload, mirroring what a user does via "Nuovo Processo." |
| [03:56:01] | definition | Stored-procedure wrapper mechanism | The same "create process" capability is also exposed as a SQL stored procedure that doesn't call the web service directly — it writes a row into a staging table ("chiamate API") that a background engine inside BPM then reads and turns into the real web-service call. |
| [03:57:06] | definition | Triggers — the only way to catch SAM-side events | Because SAM has no native outbound webhook/event mechanism, a classic SQL trigger on a SAM table (e.g. on order insert) is currently the only tool available to catch an event and kick off a BPM process. |
| [03:59:34] | tip | Trigger/stored-procedure calls are fire-and-forget (async) | Unlike a direct web-service call (which returns a pass/fail result), triggers and the stored-procedure wrapper are asynchronous — errors aren't immediately visible, so this path needs thorough testing. |
| [04:02:34] | tutorial-step | "External ID" convention for trigger-started processes | Convention adopted: the stored procedure only takes one parameter, the document's ID, mapped into a BPM process variable literally called "external ID," to keep the trigger as simple as possible. |
| [04:08:46] | tip | Recap: four aspects of SAM↔BPM integration | Summary: (1) BPM→SAM writing documents/entities via the active connector, (2) BPM reading SAM tables via connector/table views, (3) SAM→BPM launching processes via web service/SP/trigger, (4) SAM showing the BPM to-do list inline. |
| [04:13:04] | example | Anecdote: a trigger event too "fuzzy" to automate reliably | Real anecdote about a client wanting a process step auto-advanced "when the BOM (distinta base) is inserted" — the instructor explains why such vaguely-defined business events are hard to capture reliably with triggers, and why over-promising full automation to clients is risky. |
| [04:18:05] | concept-intro | Process planning/scheduling topic introduced | Introduces the "pianificazione" (Gantt-style planning) subsystem covering task durations, deadlines, and progress tracking. |
| [04:22:29] | tutorial-step | Demo: setting standard task durations on the model | Each task's Pianificazione e Scadenze tab lets you set a default duration in calendar days (5, 7, 15, 2 days in the demo) — wall-clock time, not effort/capacity; BPM has no resource-capacity concept. |
| [04:25:01] | definition | Auto-generated Gantt chart | As soon as a process instance starts, BPM automatically produces a Gantt-chart estimate of the whole process's timeline based on the configured default durations, in the variable warehouse's Pianificazione tab. |
| [04:27:32] | demo-start | Completing a task updates the Gantt live | After marking task 1 complete quickly, the Gantt shows a dashed bar (original plan) vs. the compacted actual bar, and pushes all downstream tasks' estimated dates earlier accordingly. |
| [04:28:42] | tip | Plan always pushes right, never pulls left | Key behavior: as actual progress runs later than planned, the Gantt's "updated" schedule slips later (pushes right); it never auto-compresses earlier than the standard durations, even if you're ahead. |
| [04:29:17] | definition | "Data scadenza" (deadline) concept | A separate concept from duration: a hard deadline pin (red dot in the Gantt) can be set on a task, e.g. manually or driven by a variable, representing an externally-imposed constraint to compare against the live schedule. |
| [04:33:15] | definition | Three parallel date sets: initial, updated, actual | For every task BPM tracks three copies of the dates: "programmazione" (original baseline), "aggiornamento" (current live estimate), and "effettiva" (actual — populated only once a task starts/finishes). |
| [04:36:28] | definition | "Rileva inizio" flag decouples start from activation | By default a task's start = the moment the prior task finished; enabling "rileva inizio" on a task instead requires an explicit user action to mark it started (giving an "Inizia" button instead of jumping straight to "Esegui"), useful for work that's "ready but not yet begun" (e.g. engineering design). |
| [04:42:40] | concept-intro | Linking deadlines/durations to variables | Both the deadline date and the planned duration of a task can be bound to a process variable instead of a fixed value, making the plan dynamic based on process data. |
| [04:48:36] | concept-intro | Dashboard tool introduced | Introduces "cruscotti" (dashboards): another embedded third-party (DevExpress-based) component for building synthetic visual reports over process data, separate from the pixel-perfect PDF report tool. |
| [04:50:08] | tutorial-step | Demo: create a dashboard and add a data source | Building a new dashboard from scratch: adding a data source bound to a process (e.g. "reclamo"/complaint process) and pulling in all its variables as available fields. |
| [04:55:18] | tutorial-step | Demo: combo-box filter element | Adding a "casella combinata" (combo box) bound to a date field as an interactive filter control that cross-filters the other dashboard widgets when a value is picked. |
| [04:57:35] | tip | Dashboards complement, not replace, dedicated BI tools | The instructor is clear the built-in dashboard tool isn't meant to compete with real BI platforms — its advantage is convenience: the data is already modeled and connected inside the BPM process, no external extraction needed. |
| [04:58:30] | example | KPI example: response time from two captured dates | To compute a meaningful "response time" KPI (not just raw task execution time), the process explicitly captures a "data presa in carico" and "data risposta" via Set Variable operations at specific transitions. |
| [04:59:13] | tutorial-step | Set-Variable operations capture "today" at key transitions | Demo of attaching a Set Variable operation to specific task transitions (e.g. entering "presa in carico," entering "chiusura reclamo") that stamps the current date into a variable automatically, with no user input. |
| [05:04:36] | example | Real project: coffee-roasting quality-control dashboard | Anecdote about an unusual quality-control project (coffee tasting/roasting) where dashboards were used extensively to visualize scored characteristics collected during the process. |
| [05:05:11] | definition | Dashboards live under "Analisi Dati," also available on web | Once saved, a dashboard becomes available to end users (without the design toolbar) under the Analisi Dati menu, and the same dashboard is automatically available in the web client too. |
| [05:11:08] | concept-intro | "Da confermare" (to-be-confirmed) planning concept | A per-task flag meaning the process owner gets one opportunity, right when the task becomes available, to manually adjust that instance's planned dates before "confirming" and freezing them as the new baseline (version 0). |
| [05:12:43] | demo-start | Demo: confirming freezes the baseline plan | Live demo: the owner drags/adjusts the pending task's dates, then clicks Confirm; from that point the adjusted plan becomes the fixed "version 0" baseline, and all later drift is tracked only in the "updated" schedule from then on. |
