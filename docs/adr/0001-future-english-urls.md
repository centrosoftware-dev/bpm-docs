# Preserve Italian URLs and publish future English documentation under /en/

Italian is the canonical documentation language and remains at the existing site root. When English documentation is introduced, it will be published under `/en/` so existing Italian links remain stable while each language can have its own navigation, search index, and MkDocs build.
